# Healthy Meals (Flutter)

Scaffold-only Flutter shell for the meal-tracking app. Authentication, persistence, and product screens are intentionally **not implemented** yet.

## Requirements

Flutter **3.41.5** (Dart **3.11.x**) — see repo root [.tool-versions](/.tool-versions).

## Commands

```bash
flutter pub get
flutter analyze
flutter test
flutter run
```
