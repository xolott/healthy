Flutter integration tests hook.

Run from repository root:

```bash
cd apps/healthy_meals && flutter integration_test/integration_test_smoke.dart
```

(Concrete integration tests land in future plans.)
