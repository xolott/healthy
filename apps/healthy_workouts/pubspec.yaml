publish_to: "none"
name: healthy_workouts
description: Healthy Workouts — Flutter app scaffold for the Healthy monorepo.

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.2
  http: ^1.6.0
  intl: ^0.20.2
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
